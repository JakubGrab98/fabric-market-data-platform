# Silver notebooks

Cleaning, deduplication, and standardization (currencies, dates, units)
across both batch and streaming Bronze inputs. Upsert/merge into Delta —
must be idempotent for a given date/parameter set.
