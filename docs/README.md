# Docs

- `data-model.md` — Gold star schema reference (table/column definitions)
- `source-log.md` — per-source retrieval log (API version, licensing, dates)
- `adr/` — architecture decision records
- `next-steps.md` — living checklist of open follow-ups across sessions; update it, don't let it
  go stale
- `superpowers/specs/`, `superpowers/plans/` — design specs and implementation plans for
  individual features, one pair per feature
